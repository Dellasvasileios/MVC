# MVC
MVC FRAMEWORK

# Install dependencies
composer install (inside folder www)
1. php unit
2. autoloader

# RUN TESTS

./vendor/bin/phpunit

# Run with docker
1) docker-composer up -d --build
2) http://localhost:80





